# MongoDB connection string used to persist scan and duplicate results.
# Optional — without it the app still works, but results reset on restart.
MONGODB_URI="mongodb+srv://<user>:<password>@<cluster>/drivespace?retryWrites=true&w=majority"
